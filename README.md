# Hybrid-LoRA-DomainAdaptation-GPT2
Implementation of Dynamic Low-Rank Adaptation (DyLoRA) and Domain-Correlation LoRA (DC-LoRA) on GPT-2 for efficient cross-domain fine-tuning, reducing catastrophic forgetting across NLP domains (IMDb → Yelp).
